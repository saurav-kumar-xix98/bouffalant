#pragma once

#include <vector>
#include <cstdint>

namespace CF1552FTelepanting {
    int32_t telepanting(const std::vector<int32_t> &src, const std::vector<int32_t> &des, const std::vector<bool> &state);
}
